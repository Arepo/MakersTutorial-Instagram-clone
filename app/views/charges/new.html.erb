<div class="container">
	<%= form_tag new_post_charge_path(@post) do %>
		<article>
			<label class="amount">
				<span>Amount: £10.00</span>
			</label>
		</article>

		<script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
				data-key="<%= Rails.application.secrets.stripe_public_key %>"
				data-description="Photo of probable kitten"
				data-amount="1000">
		</script>
	<% end %>
</div>